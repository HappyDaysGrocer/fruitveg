/* Live bank feed REMOVED from this public file for security — loads at runtime
   from the locked Firebase /finance node (only the 6 team logins can read it).
   Empty stubs below prevent errors before that load. */
var BANK_FEED_META = { from: '', to: '', income: 0, expense: 0, count: 0 };
var BANK_FEED      = [];
